# vendor/ggui

This directory is reserved for modified GGUI fork packages only.

Do not mirror the full upstream repository here by default. When Jium needs a local patch to a GGUI package, copy that package into this directory and wire it through `pnpm.overrides` or a workspace dependency.
