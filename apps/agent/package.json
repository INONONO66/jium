{
  "name": "@jium/agent",
  "version": "0.0.1",
  "description": "Jium OpenAI Agents SDK backend that orchestrates GGUI rendering and Jium MCP services.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@ggui-ai/agent-server": "0.2.0-alpha.4",
    "@ggui-ai/protocol": "0.2.0-alpha.4",
    "@openai/agents": "^0.0.13",
    "dotenv": "^16.4.5",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0"
  }
}
